_schema-version: "3.2"
ID: comskproducts
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comskproducts-destination-content
  type: com.sap.application.content
  requires:
  - name: comskproducts-destination-service
    parameters:
      content-target: true
  - name: comskproducts-repo-host
    parameters:
      service-key:
        name: comskproducts-repo-host-key
  - name: comskproducts-uaa
    parameters:
      service-key:
        name: comskproducts-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comskproducts_html_repo_host
          ServiceInstanceName: comskproducts-html5-service
          ServiceKeyName: comskproducts-repo-host-key
          sap.cloud.service: comskproducts
        - Authentication: OAuth2UserTokenExchange
          Name: comskproducts_uaa
          ServiceInstanceName: comskproducts-xsuaa-service
          ServiceKeyName: comskproducts-uaa-key
          sap.cloud.service: comskproducts
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comskproducts-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comskproducts-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comskproducts.zip
      name: comskproducts
      target-path: resources/
- name: comskproducts
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comskproducts-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comskproducts-destination-service
    service-plan: lite
- name: comskproducts-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comskproducts-xsuaa-service
    service-plan: application
- name: comskproducts-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comskproducts-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
